/* Creator catalog share URL: /c/<slug>. See p.$slug.tsx for routing
 * notes — same SPA shell, the slug is consumed by useOverlayRouter
 * which opens the matching CreatorPage. */
export { default } from './_index';
